1．空字典的代码是怎样的？
a={}
2．一个字典包含键'fow'和值42，看起来是怎样的？
{'fow':42}
3．字典和列表的主要区别是什么？
有序无序  下标可以是任何
4．如果spam 是{'bar': 100}，你试图访问spam['foo']，会发生什么？
找不到会报错 
5．如果一个字典保存在spam 中，表达式'cat' in spam 和'cat' in spam.keys()之间
的区别是什么？
无区别
6．如果一个字典保存在变量中，表达式'cat' in spam 和'cat' in spam.values()之间
的区别是什么？
第一个判断key的存在  第二个判断值的存在
7．下面代码的简洁写法是什么？
if 'color' not in spam:
spam['color'] = 'black'

spam.setdefault(color,'black')
8．什么模块和函数可以用于“漂亮打印”字典值？
pprint.pprint