1．创建Regex 对象的函数是什么？
compile()
2．在创建Regex 对象时，为什么常用原始字符串？
增加代码可读性，
3．search()方法返回什么？
返回一个match对象
4．通过Match 对象，如何得到匹配该模式的实际字符串？
使用group()函数
5．用r'(\d\d\d)-(\d\d\d-\d\d\d\d)'创建的正则表达式中，分组0 表示什么？分组1
呢？分组2 呢？
0表示整个串  1表示第一组 2表示第二组分组串
6．括号和句点在正则表达式语法中有特殊的含义。如何指定正则表达式匹配真正的括号和句点字符？
用转义符\
7．findall()方法返回一个字符串的列表，或字符串元组的列表。是什么决定它
提供哪种返回？
是否分组
8．在正则表达式中，|字符表示什么意思？
匹配任意一种情况
9．在正则表达式中，?字符有哪两种含义？
匹配零次或一次
10．在正则表达式中，+和*字符之间的区别是什么？
+匹配零次或多次   ×匹配一次或多次
11．在正则表达式中，{3}和{3,5}之间的区别是什么？
{3}匹配固定3次  {3,5}匹配3～5次
12．在正则表达式中，\d、\w 和\s 缩写字符类是什么意思？
匹配 数字 字母 
13．在正则表达式中，\D、\W 和\S 缩写字符类是什么意思？
匹配 非数字 非字母 
14．如何让正则表达式不区分大小写？
使用参数
15．字符.通常匹配什么？如果re.DOTALL 作为第二个参数传递给re.compile()，
它会匹配什么？
.通常匹配一个任意字符  re.DOTALL 表示用.结尾
16．.*和*?之间的区别是什么？

17．匹配所有数字和小写字母的字符分类语法是什么？
18．如果numRegex = re.compile(r'\d+')，那么numRegex.sub('X', '12 drummers, 11
pipers, five rings, 3 hens')返回什么？
数字统统用x代替了
19．将re.VERBOSE 作为第二个参数传递给re.compile()，让你能做什么？
让表达式更易读
20．如何写一个正则表达式，匹配每3 位就有一个逗号的数字？它必须匹配以
下数字：
 '42'
 '1,234'
 '6,368,745'
但不会匹配：
 '12,34,567' （逗号之间只有两位数字）
 '1234' （缺少逗号）
\d{1,3}(\,\d*3)+
21．如何写一个正则表达式，匹配姓Nakamoto 的完整姓名？你可以假定名字
总是出现在姓前面，是一个大写字母开头的单词。该正则表达式必须匹配：
 'Satoshi Nakamoto'
 'Alice Nakamoto'
 'RoboCop Nakamoto'
但不匹配：
 'satoshi Nakamoto'（名字没有大写首字母）
 'Mr. Nakamoto'（前面的单词包含非字母字符）
 'Nakamoto' （没有名字）
 'Satoshi nakamoto'（姓没有首字母大写）
22．如何编写一个正则表达式匹配一个句子，它的第一个词是Alice、Bob 或Carol，第二个词是eats、pets 或throws，第三个词是apples、cats 或baseballs。该句
子以句点结束。这个正则表达式应该不区分大小写。它必须匹配：
 'Alice eats apples.'
 'Bob pets cats.'
 'Carol throws baseballs.'
 'Alice throws Apples.'
 'BOB EATS CATS.'
但不匹配：
 'RoboCop eats apples.'
 'ALICE THROWS FOOTBALLS.'
 'Carol eats 7 cats.'